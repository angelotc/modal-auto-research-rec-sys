[project]
name = "auto-tiger"
version = "0.1.0"
description = "Modal-native autoresearch-style TIGER experiment harness"
requires-python = ">=3.11"
dependencies = [
  "deeplake",
  "matplotlib",
  "modal",
  "numpy",
  "pandas",
  "pyarrow",
  "torch",
]

[tool.uv]
package = false
